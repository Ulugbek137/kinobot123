services:
  - type: web
    name: flask-kino-bot
    env: python
    region: oregon
    plan: free
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: python flask_kino_bot_fixed.py
    envVars:
      - key: PORT
        value: 10000
    autoDeploy: true
